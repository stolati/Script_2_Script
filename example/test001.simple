Module(
  name("test001"),
  expressions([
    affectation(
      name('UIObject'),
      Klass(
        [] //variables
        ,
        [
          affectation(
            name('getElement'),
            function(
              Type_obj_fct(),
              , [param_this('self')]
              , expressions([
                  return(
                    follow_path(name('self'), name('element'))
                  ) 
              ])

          ),
          affectation(
            name('setElement'),
            function(
              Type_obj_fct(),
              [param_this('self'), param('element')],
              expressions([
                affectation(
                  follow_path(name('self'), name('element')),
                  name('element')
                )
              ])

             )
          )
        ] //functions
        )
    ]
